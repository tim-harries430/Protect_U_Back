# 《PUB 法典》 — 一座 AI 门卫王国的全部规矩

> **给所有人看(含不懂技术的)。** 大白话在前,英文大写的"判罚码"在后(那是代码里真实的罪名,给工程师对照用——也是这本法典每一条的出处:全是从代码里抽的,不是编的)。

---

## 这是什么(一句话)

你的电脑上有个 AI 助手(agent)在帮你干活——写代码、删文件、装东西、联网。**pub 就是站在它和你电脑之间的一个门卫**:AI 每要做一件事,**先问 pub**,pub 看一眼放不放。

门卫的三个动作:

- **放行(PASS)**——正常,过。
- **暂停(HOLD)**——可疑,拦下,**等你点头**才行(不点就不动)。
- **击杀(KILL)**——危险,**直接拒绝**,门都不开。

---

## 门卫的脾气(7 条总原则)

1. **拿不准,就拦。** 看不透、证据不全、说不清——一律不放(宁可错拦,不可放过)。
2. **谁严听谁的。** 好几个判官一起看一件事,**只要有一个说"危险",就按危险办**。说"没事"的那个,**永远压不过**说"危险"的那个。
3. **只会越管越严,不会越管越松。** 后面几道判官只能把判决往"更拦"抬,永远不能放松。
4. **AI 自己说'可以'不算数。** 必须 pub 点头才行。
5. **看人下菜。** 同一条命令,不同的 AI 助手,门卫的尺度可以不一样(信得过的放宽点,信不过的照拦)。
6. **删得回来才让删。** 破坏性操作,只在"**能先备份、删错能还原**"时才放;备份不了的,拦。
7. **只信看得见的,不信嘴上说的。** 门卫看的是文件真实的指纹和身份证,不信任何"我是好人"的自我声明。

---

## 王国里的判官(12 位,各管一摊)

> 每位判官底下,中文是它管的事,英文大写是它能开的"罚单名"。

### 1. OT 关 · 三位主判官(`ot_gate.py`)—— 王国的宪法庭
>
> **大白话:管"这条命令越没越界、安不安分"。** 三个人一起看:① 边界(碰没碰不该碰的地方)② 意图(说的和做的一不一致)③ 证据(你是谁、有没有授权)。

边界这块,这些一律**直接拒绝(KILL)**:

- 删/改受保护的核心区 `CRITICAL_PROTECTED_PHI_WRITE` · 碰登记册 `CRITICAL_REGISTRY_READ`
- 危险删除(不是"删一个项目内小文件"那种)`CRITICAL_DESTRUCTIVE_COMMAND`
- 提权(拿管理员权限)`CRITICAL_PRIVILEGE_ESCALATION`
- 一边下载一边执行(典型木马手法)`CRITICAL_NETWORK_EXECUTE`
- 碰密钥 `CRITICAL_SECRET_ACCESS` · 改审计/权限 `CRITICAL_AUDIT_BYPASS`
- 往**项目外**写/删 `CRITICAL_EXTERNAL_WRITE`

这些是**暂停问你(HOLD)**:读受保护区 `PROTECTED_PHI_READ_REQUIRES_APPROVAL` · 读项目外 `EXTERNAL_READ_REQUIRES_APPROVAL` · 动环境变量/联网 `ENV_OR_NETWORK_REQUIRES_APPROVAL`。
身份不清也拦:`MISSING_ACTOR_ID`(不知道你是谁)· `UNKNOWN_ACTOR`(没登记)· `ACTOR_FROZEN`(你之前犯过事,被冻结了)。

### 2. 能力墙(`capability_wall.py`)—— 查"你有没有这个权限"
>
> **大白话:每个 AI 入职时领一张"能力清单",写明它能干嘛。超出清单的,拦。**

超权限删 `CAPABILITY_SIDE_EFFECT_DENIED` · 越界提权/改审计 `CAPABILITY_PERMISSION_MUTATION_DENIED`/`_AUDIT_MUTATION_DENIED` · 碰清单外的路径 `CAPABILITY_PATH_DENIED` · 联陌生域名 `CAPABILITY_NETWORK_DOMAIN_DENIED` · 伪造内部凭证 `CAPABILITY_PROBE_MINT_UNAUTHORIZED` · 该走的安全步骤没走 `CAPABILITY_SKILL_REQUIRED_STEP_SKIPPED`。

### 3. 双court 合议(`parallel_audit.py`)—— 两个庭一起开,谁严算谁的
>
> **大白话:一个庭看"命令本身长啥样",另一个庭看"它会造成什么后果",两边意见一汇总,从严。**

看不透的命令 `COMMAND_SURFACE_OPAQUE_EXECUTION`(见判官 4)· 不认识的命令 `UNKNOWN_COMMAND_SURFACE` · 两庭吵起来了 → 从严暂停 `DUAL_COURT_CONFLICT_HOLD`。

### 4. "看不透"识别器(`opaque_executor.py`)
>
> **大白话:一段 `python ...`、`bash 某脚本`、裸 `> 文件`——门卫没法看穿里面到底要干嘛。看不透的,不杀,但先暂停(HOLD),别瞎猜。**

### 5. X 光透视(`transition_xray.py` 等)—— 看文件的"真身"
>
> **大白话:有人把病毒改个 `.txt` 后缀想蒙混?门卫不看后缀,看文件的真实指纹。**

伪装成别的类型、藏数据流(ADS)、改时间戳、偷梁换柱、硬链接别名(`HARD_LINK_ALIAS`)、进出文件指纹对不上(被人半路改了)`FRAME_DRIFT_MISMATCH`——全照穿。

### 6. 第四判官 · 看"连续动作"(`temporal_continuity.py`)
>
> **大白话:单看一步都正常,连起来就不对劲——"先读了密钥,又下载了个东西,然后要往外发"。这位专盯这种串起来的坏。** 只会越盯越严。

累积暴露要外泄 `TEMPORAL_ACCUMULATED_EXPOSURE_EXIT` · 污点外传 `TEMPORAL_TAINT_EGRESS`。

### 7. 环境判官 · 看"周围有没有被动手脚"(`scene_continuity.py`)
>
> **大白话:在两个动作之间,pub 自己的看门代码有没有被人偷偷改了?有 → 暂停。**(注:项目里的普通文件你随便删随便建,它**不管**——那是你干活的正常动作。)

pub 模块被带外篡改 `SCENE_DISCONTINUOUS`。

### 8. 防护扫描(`protect_scan.py`)—— 守你私人电脑上的命门
>
> **大白话:这位专门护着你电脑上最敏感的东西——密钥、密码、浏览器资料、邮件、个人文档、pub 自己的代码。碰它们,要么暂停问你,要么直接拒。**

外传密钥 `PROTECT_SECRET_SURFACE_DENIED`/`PROTECT_NETWORK_EXFIL_DENIED` · 改 pub 自己 `PROTECT_PUB_INTERNAL_MUTATION_DENIED` · 动 `.git` 钩子(可塞后门)`PROTECT_GIT_SURFACE_WRITE_DENIED` · 个人文档/浏览器/邮件 `PROTECT_PERSONAL_DOCUMENT_*`/`_BROWSER_PROFILE_*`/`_EMAIL_CACHE_*`(碰=拒,读=问) · 偷偷削墙的补丁 `PATCH_AUDIT_SILENT_BOUNDARY_WEAKENING`。

### 9. 绿色通道(`llm_channel.py`)—— 给"明显安全的日常活"开快车
>
> **大白话:`git commit` 这种安全的日常操作,给它开个绿色通道直接过——但只要沾一点危险(联网、密钥、删项目外、看不透),立刻取消通道、打回去。** 这是"加分项",只会放、绝不会帮坏人。

### 10. 门房登记 + 史官(`registry_admission` · `event_ledger` · `task_guard`)
>
> **大白话:进门先登记(你是谁、要干嘛);史官记账——同一个 Agent **连犯两次重罪就冻结它**;一直犯,**直接终止这次任务**。**

两次致命操作 → 冻结 `BUGCHECK` · 反复杀 → `TASK_TERMINATED`。

### 11. 牢房与笼子(`pub_os_*`)—— 最后的兜底:把 AI 关进隔间
>
> **大白话:除了门卫审,还能直接把 AI **关进一个隔间(笼子)**——它只能动这一个项目,碰不到你电脑别处,就算门卫漏判,它也跑不出去。** 笼子起不来就**宁可不开工**,绝不裸跑。

见证丢了 `PUB_OS_LEDGER_WITNESS_LOST`(HOLD) · 删前备份失败 `PUB_BACKFILL_JOURNAL_FAILED`(HOLD) · 笼子建不成 `cage_unavailable:*`(fail-closed)。

### 12. 访问物理 / 时栅(`access_*`)—— 底层的"能不能动"演算
>
> **大白话:一套底层公式,算"这个文件此刻动得动不动"(它有多少权限、暴露了多少、有多可信)。还盯文件的身份漂移——身份证号变了、指针指向变了、时间戳被人改了,都报。** 不用细看,门卫内部用。

---

## 什么时候会"减刑"(放行的白名单 + 它的安全锁)

> **大白话:门卫不是只会拦。下面这些"明显安全"的日常活,会放——但每条都配一把安全锁,锁一不上,立刻打回去拦住。放行用"白名单"(只列确定安全的),漏列一个顶多是"被多拦了一下",绝不会变成漏洞。**

1. **删/改名一个项目里的小文件**:是真的删除/改名命令 + 目标都在项目内 + 不是递归/通配/家目录 → 放。**但裸 `> 文件`(清空)这种没动词的,照拦。** 只对信得过的 `claude_code`。
2. **删一个项目里的文件夹**(递归):项目内、数得清里面有啥 → 放,**但放之前必须先把里面每个文件备份一份**;备份不了(太大/数不清)→ 暂停。
3. **删的时候留个底**:第一次放行这种删除,在"尸检"目录留一条记录,事后能查。
4. **`git` 日常操作**(add/commit/checkout/...):单条、不带 `--hard`/`--force` 这种危险开关 → 放;`reset --hard`、`push`、`clean` 照拦。
5. **项目内 chmod/tar/zip**:普通操作放;改成 setuid、碰项目外的,拦。**`ln`(建链接)不放**——它能造逃逸,留在墙里。

---

## 几条压舱的元规矩

- **谁严算谁的**:多判官一汇总,取最严那个;两庭吵架 → 从严暂停。
- **只升不降**:门卫审完,后面的"连续动作判官 → 环境判官 → 牢房见证"只能把判决往更严抬,绝不放松。
- **目录豁免**:文件夹天生"链接数 ≥ 2",那是正常结构,**不当**别名嫌疑(今晚刚修的)。
- **两条祖训**:① 别去"列举所有坏的"——那永远列不全、必漏;拦的一侧一律"默认不放"。② 别信嘴上说的——只锚物理事实。
- **墙 > 关 > 出口**:先有能兜底的笼子(墙),再有判官(关),最后才谈让活儿顺畅地出去(出口);**永远不为了让活儿顺,去松上面的墙。**

---

_这本法典的每一条罪名,都是代码里真实存在的判罚码(单一真相源)。改一条判罚,就回来改一条法。_
